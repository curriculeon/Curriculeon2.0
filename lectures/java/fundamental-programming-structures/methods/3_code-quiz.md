---
layout: code-quiz
---


# Quiz


```java
public class PersonGreeter {
    public static void greet(String name, int age) {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
}
```




---
#primaryColor: steelblue
#shuffleQuestions: true
shuffleAnswers: true
---


### What is the class signature?

- [X] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [ ] `void`
- [ ] `public`
- [ ] `greet`
- [ ] `String name, int age`
- [ ] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```


### What is the class name?


- [ ] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [X] `PersonGreeter`
- [ ] `void`
- [ ] `public`
- [ ] `greet`
- [ ] `String name, int age`
- [ ] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```



### What is the method signature?

- [ ] `public class PersonGreeter`
- [X] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [ ] `void`
- [ ] `public`
- [ ] `greet`
- [ ] `String name, int age`
- [ ] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```


### What is the method name?

- [ ] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [ ] `void`
- [ ] `public`
- [X] `greet`
- [ ] `String name, int age`
- [ ] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```


### What is the method accessibility?

- [ ] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [ ] `void`
- [X] `public`
- [ ] `greet`
- [ ] `String name, int age`
- [ ] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```

### What is the method return-type?

- [ ] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [X] `void`
- [ ] `public`
- [ ] `greet`
- [ ] `String name, int age`
- [ ] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```


### What is the method's parameter?

- [ ] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [ ] `void`
- [ ] `public`
- [ ] `greet`
- [X] `String name, int age`
- [ ] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```



### What does the method take an argument of?

- [ ] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [ ] `void`
- [ ] `public`
- [ ] `greet`
- [ ] `String name, int age`
- [X] a `String` value and an `int` value
- [ ] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```



### What is the method body?

- [ ] `public class PersonGreeter`
- [ ] `public static void greet(String name, int age)`
- [ ] `PersonGreeter`
- [ ] `void`
- [ ] `public`
- [ ] `greet`
- [ ] `String name, int age`
- [ ] a `String` value and an `int` value
- [X] 
    ```java
    {
        String nameGreeting = "My name is %s."
        String ageGreeting = "My age is %s."
        String unformattedMessage = nameGreeting + ageGreeting;
        String formattedMessage = String.format(unformattedMessage, name, age);
        System.out.println(formattedMessage);
    }
    ```


### What is the type of the variable `name`?
- [ ] `int`
- [ ] `String[]`
- [X] `String`
- [ ] `void`
- [ ] `PersonGreeter`
- [ ] `greet`
- [ ] `formattedMessage`

### What is the type of the variable `age`?
- [X] `int`
- [ ] `String[]`
- [ ] `String`
- [ ] `void`
- [ ] `PersonGreeter`
- [ ] `greet`
- [ ] `formattedMessage`


### What is the type of the variable `nameGreeting`?
- [ ] `int`
- [ ] `String[]`
- [X] `String`
- [ ] `void`
- [ ] `PersonGreeter`
- [ ] `greet`
- [ ] `formattedMessage`


### What is the type of the variable `ageGreeting`?
- [ ] `int`
- [ ] `String[]`
- [X] `String`
- [ ] `void`
- [ ] `PersonGreeter`
- [ ] `greet`
- [ ] `formattedMessage`